# zapier
Zapier app

### Bump version
1. Update version in package.json
2. `zapier push`

### Build
1. `yarn build`
2. `zapier build`
3. `zapier push`

### Log
1. `zapier logs`
2. `zapier logs --type=console`
3. `zapier logs --type=http --detailed`
